import { useEffect, useRef, useState } from "react";
import { createRoot } from "react-dom/client";
import * as THREE from "three";
import { OrbitControls } from "three/examples/jsm/controls/OrbitControls.js";
import "./styles.css";
type Site={kind:string;coordinate:number[];key:string};type Atom={id:number;site:Site;state:string};type Metrics={vacancies:number;interstitials:number;defects:number;entropy:number};type Snapshot={revision:number;dimensions:number[];mode:"2d"|"3d";atoms:Atom[];metrics:Metrics;act:number;can_undo:boolean;can_redo:boolean;history_index:number;history_length:number};type Event={act:number;revision:number;energy_ev:number;event_type:string;target_atom_id:number};
const palette:Record<string,string>={correct:"#5b8def",surface:"#9a65c9",interstitial:"#ed8a34",surface_defect:"#b84d83"};
async function api<T>(path:string,body?:object):Promise<T>{const response=await fetch(path,{method:body?"POST":"GET",headers:{"Content-Type":"application/json"},body:body?JSON.stringify(body):undefined});if(!response.ok)throw new Error((await response.json()).detail??"Request failed");return response.json() as Promise<T>}
function Lattice2D({snapshot,selectedAtomId}:{snapshot:Snapshot;selectedAtomId:number|null}){const canvasRef=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=canvasRef.current;if(!canvas)return;const bounds=canvas.getBoundingClientRect(),ratio=Math.min(window.devicePixelRatio,2);canvas.width=bounds.width*ratio;canvas.height=bounds.height*ratio;const c=canvas.getContext("2d");if(!c)return;c.scale(ratio,ratio);c.clearRect(0,0,bounds.width,bounds.height);const[nx,ny]=snapshot.dimensions,padding=34,step=Math.min((bounds.width-padding*2)/Math.max(nx-1,1),(bounds.height-padding*2)/Math.max(ny-1,1)),width=step*(nx-1),height=step*(ny-1),ox=(bounds.width-width)/2,oy=(bounds.height-height)/2;c.strokeStyle="#273348";for(let x=0;x<nx;x+=1){c.beginPath();c.moveTo(ox+x*step,oy);c.lineTo(ox+x*step,oy+height);c.stroke()}for(let y=0;y<ny;y+=1){c.beginPath();c.moveTo(ox,oy+y*step);c.lineTo(ox+width,oy+y*step);c.stroke()}for(const atom of snapshot.atoms){const[x,y]=atom.site.coordinate,px=ox+x*step,py=oy+y*step;if(atom.id===selectedAtomId){c.beginPath();c.fillStyle="#f7d654";c.arc(px,py,Math.max(9,step*.34),0,Math.PI*2);c.fill()}c.beginPath();c.fillStyle=palette[atom.state]??palette.correct;c.arc(px,py,Math.max(3,Math.min(7,step*.22)),0,Math.PI*2);c.fill()}},[selectedAtomId,snapshot]);return <canvas className="scene scene-2d" ref={canvasRef} aria-label="Вид сверху на 2D кристаллическую решётку"/>}
function Lattice3D({snapshot,selectedAtomId}:{snapshot:Snapshot;selectedAtomId:number|null}){const host=useRef<HTMLDivElement>(null);useEffect(()=>{const container=host.current;if(!container)return;const scene=new THREE.Scene();scene.background=new THREE.Color("#10151e");const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(window.devicePixelRatio,2));renderer.setSize(container.clientWidth,container.clientHeight);container.replaceChildren(renderer.domElement);const[nx,ny,nz]=snapshot.dimensions,max=Math.max(nx,ny,nz),camera=new THREE.PerspectiveCamera(45,container.clientWidth/container.clientHeight,.1,400);camera.position.set(max*1.25,max*1.05,max*1.35);const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.minDistance=max*.6;controls.maxDistance=max*4;scene.add(new THREE.AmbientLight(0xffffff,1.5),new THREE.DirectionalLight(0xb7ccf0,1.2),new THREE.GridHelper(max,max,0x33445b,0x263244));const geometry=new THREE.SphereGeometry(.19,16,12);for(const atom of snapshot.atoms){const[x,y,z]=atom.site.coordinate,sphere=new THREE.Mesh(geometry,new THREE.MeshStandardMaterial({color:palette[atom.state]??palette.correct,roughness:.35,metalness:.25}));sphere.position.set(x-(nx-1)/2,z-(nz-1)/2,y-(ny-1)/2);scene.add(sphere);if(atom.id===selectedAtomId){const mark=new THREE.Mesh(new THREE.SphereGeometry(.29,16,12),new THREE.MeshBasicMaterial({color:"#f7d654",wireframe:true}));mark.position.copy(sphere.position);scene.add(mark)}}let frame=0;const draw=()=>{controls.update();renderer.render(scene,camera);frame=requestAnimationFrame(draw)};draw();const resize=()=>{camera.aspect=container.clientWidth/container.clientHeight;camera.updateProjectionMatrix();renderer.setSize(container.clientWidth,container.clientHeight)};window.addEventListener("resize",resize);return()=>{cancelAnimationFrame(frame);window.removeEventListener("resize",resize);controls.dispose();geometry.dispose();renderer.dispose()}},[selectedAtomId,snapshot]);return <div className="scene" ref={host} aria-label="Вращаемая 3D кристаллическая решётка"/>}
function AxisInputs({mode,dimensions,onChange}:{mode:"2d"|"3d";dimensions:number[];onChange:(i:number,v:number)=>void}){return <div className="axis-inputs">{["X","Y","Z"].slice(0,mode==="2d"?2:3).map((axis,i)=><label key={axis}>{axis}<input aria-label={`Размер оси ${axis}`} type="number" min="2" max={mode==="2d"?200:30} value={dimensions[i]} onChange={e=>onChange(i,Number(e.target.value))}/></label>)}</div>}
function App(){const[mode,setMode]=useState<"2d"|"3d">("2d"),[dimensions,setDimensions]=useState([10,10,10]),[moved,setMoved]=useState(0),[seed,setSeed]=useState(42),[snapshot,setSnapshot]=useState<Snapshot|null>(null),[events,setEvents]=useState<Event[]>([]),[selectedAtomId,setSelectedAtomId]=useState<number|null>(null),[error,setError]=useState(""),[simulation,setSimulation]=useState<"paused"|"forward"|"reverse">("paused");const busy=useRef(false);const create=async()=>{try{setError("");setEvents([]);setSelectedAtomId(null);setSimulation("paused");setSnapshot(await api<Snapshot>("/api/model",{dimensions:dimensions.slice(0,mode==="2d"?2:3),moved_atoms:moved,seed_init:seed,seed_sim:seed+1}))}catch(reason){setError(reason instanceof Error?reason.message:"Ошибка создания модели")}};const step=async()=>{const event=await api<Event>("/api/model/step",{});setEvents(items=>[event,...items].slice(0,80));setSelectedAtomId(event.target_atom_id);setSnapshot(await api<Snapshot>("/api/model/snapshot"))};const travel=async(direction:"undo"|"redo")=>setSnapshot(await api<Snapshot>(`/api/model/${direction}`,{}));const guarded=async(action:()=>Promise<void>)=>{if(busy.current)return;try{busy.current=true;setError("");await action()}catch(reason){setSimulation("paused");setError(reason instanceof Error?reason.message:"Ошибка симуляции")}finally{busy.current=false}};useEffect(()=>{void create()},[]);useEffect(()=>{if(simulation==="paused")return;const timer=window.setInterval(()=>void guarded(async()=>{if(simulation==="forward")await step();else if(snapshot?.can_undo)await travel("undo");else setSimulation("paused")}),260);return()=>window.clearInterval(timer)},[simulation,snapshot]);useEffect(()=>{const listener=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="z"){event.preventDefault();setSimulation("paused");void guarded(()=>travel("undo"))}};window.addEventListener("keydown",listener);return()=>window.removeEventListener("keydown",listener)},[snapshot]);const selected=snapshot?.atoms.find(a=>a.id===selectedAtomId),metrics=snapshot?.metrics,axis=(i:number,v:number)=>setDimensions(values=>values.map((current,index)=>index===i?Math.max(2,v||2):current));return <main><aside className="sidebar"><div className="brand"><span className="brand-mark">g</span><div><strong>Gamma Irradiation</strong><small>qualitative lattice model</small></div></div><section><h2>Новая модель</h2><label>Пространство<select value={mode} onChange={e=>setMode(e.target.value as "2d"|"3d")}><option value="2d">2D решётка</option><option value="3d">3D решётка</option></select></label><label>Размеры решётки</label><AxisInputs mode={mode} dimensions={dimensions} onChange={axis}/><label>Перемещённые атомы<input type="number" min="0" value={moved} onChange={e=>setMoved(Number(e.target.value))}/></label><label>Seed<input type="number" value={seed} onChange={e=>setSeed(Number(e.target.value))}/></label><button className="primary" onClick={()=>void create()}>Создать модель</button></section><section><h2>Симуляция</h2><div className="simulation-controls"><button onClick={()=>void guarded(step)} disabled={!snapshot}>Шаг</button><button className={simulation==="forward"?"active-control":""} onClick={()=>setSimulation("forward")} disabled={!snapshot}>Пуск</button><button onClick={()=>setSimulation("paused")} disabled={simulation==="paused"}>Пауза</button><button title="Ctrl+Z" onClick={()=>void guarded(()=>travel("undo"))} disabled={!snapshot?.can_undo}>Назад</button><button className={simulation==="reverse"?"active-control":""} onClick={()=>setSimulation("reverse")} disabled={!snapshot?.can_undo}>Обратно</button><button onClick={()=>void guarded(()=>travel("redo"))} disabled={!snapshot?.can_redo}>Вперёд</button></div></section><p className="notice">Модель качественная: параметры требуют анализа чувствительности и не предсказывают свойства материала.</p></aside><section className="workspace"><header><div><span className="eyebrow">AUTHORITATIVE PYTHON STATE · {simulation==="paused"?"ПАУЗА":simulation==="forward"?"СИМУЛЯЦИЯ ВПЕРЁД":"ИСТОРИЯ НАЗАД"}</span><h1>{mode==="2d"?"2D: вид сверху":"3D: перетаскивайте для вращения"}</h1></div><span className="revision">act {snapshot?.act??0} · rev {snapshot?.revision??0}</span></header>{error&&<div className="error">{error}</div>}{snapshot&&(mode==="2d"?<Lattice2D snapshot={snapshot} selectedAtomId={selectedAtomId}/>:<Lattice3D snapshot={snapshot} selectedAtomId={selectedAtomId}/>)}<div className="legend">{Object.entries(palette).map(([state])=><span key={state}><i className={state}/>{state}</span>)}{selected&&<span className="selected-legend"><i/>выбранный атом #{selected.id}</span>}</div><div className="metrics">{[["Акт",snapshot?.act??0],["История",`${(snapshot?.history_index??0)+1}/${snapshot?.history_length??1}`],["Вакансии",metrics?.vacancies??0],["Дефекты",metrics?.defects??0],["Энтропия",metrics?.entropy.toFixed(3)??"0.000"]].map(([label,value])=><div key={String(label)}><small>{label}</small><strong>{value}</strong></div>)}</div></section><aside className="diagnostics"><h2>Журнал актов</h2>{selected&&<div className="selected-atom"><strong>Атом #{selected.id}</strong><span>{selected.state}</span><small>({selected.site.coordinate.join(", ")})</small></div>}{events.length===0?<p className="muted">Выполните первый акт облучения.</p>:events.map(event=><button className={`event ${selectedAtomId===event.target_atom_id?"active":""}`} key={event.revision} onClick={()=>setSelectedAtomId(event.target_atom_id)}><strong>#{event.act} {event.event_type}</strong><span>Q = {event.energy_ev.toFixed(2)} eV</span><small>мишень: атом {event.target_atom_id}</small></button>)}</aside></main>}
createRoot(document.getElementById("root")!).render(<App/>);
